{
  "name": "schedule_reminder",
  "parameters": {
    "type": "object",
    "properties": {
      "text": {
        "type": "string",
        "description": "A text of the reminder (what to remind about)"
      },
      "diff_in_minutes": {
        "type": "string",
        "description": "A number of minutes to add to the current time"
      }
    },
    "required": ["text", "diff_in_minutes"]
  },
  "description": "Schedules a reminder for a given date and time"
}
